#spring.application.name=ms-registry-server
spring:
  application:
    name: ms-registry-server
  profiles:
    active: development
  config:
    import: optional:configserver:http://root:password@localhost:7070

